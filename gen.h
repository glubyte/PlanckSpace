#pragma once

// standard
#include <stdio.h>
// local
#include "maths.h"
#include "chunks.h"

// stack handles

// prototypes
void genSphere(int radius);
void testFile(unsigned long int size);